# STATE — 세션 시작 시 여기만 읽는다

> 60줄 유지. 서술 금지, 상태만. 세션 끝에 **Claude Code CLI가 갱신**한다.
> 배경/경위가 필요하면 `handoff-2026-08-15.md`, 전체 로그는 `dev-log.md`.
> 최종 갱신: 2026-08-15 (Claude 앱 / Opus)

## 3층 운영규칙

| 층 | 표면 | 하는 일 | **하면 안 되는 일** |
|---|---|---|---|
| 1 | Claude 앱 (Opus) | 결정·숫자·수용기준. `docs/spec/*.md` 작성. 레인 분할 판정 | 코드 작성, 지시서 본문 작성, 실행 검증 |
| 2 | Claude Code CLI | `main` 구현, 지시서 확장, merge, `main.js` 연결, 검증 스크립트 | 자기 코드를 육안으로 최종 판정 → 스크립트에 위임 |
| 3 | Gemini / Antigravity | 배타 소유 파일만 (`player/`, `audio/`, `world/rooms/*`) | `main.js`·`index.html`·`layout.js` 수정, `main`에 merge |

**불변 규칙**
1. 3층은 **반드시 별도 클론 폴더**에서 작업 (같은 폴더 브랜치 전환 사고 재발 방지)
2. 각 레인은 `init()` 하나만 노출. 씬 등록은 2층이 `main.js`에서
3. 작업 단위마다 즉시 commit + push. 몰아서 금지
4. 1층은 `dev-log.md`(35KB)를 읽지 않는다. 이 파일 + 필요한 소스 1~2개만
5. 품질 게이트는 사람 리뷰가 아니라 **자동 검증 스크립트** (지금까지 버그 5건 중 리뷰로 잡혔을 것 0건)

## 마일스톤

| | 상태 |
|---|---|
| M0 기반 | ✅ |
| M1 이동/충돌/카메라 | ✅ |
| M2 룩 (후처리·안개·바깥지형) | ✅ |
| M3 (a)에셋 파이프라인 ✅ / (b)걷는 캐릭터 | 🟡 |
| **M4 집 4칸** | 🔴 **다음 작업 · 임계경로.** 스펙 확정 → `docs/spec/M4-layout.md` |
| M5 배포 (itch.io) | 🔴 M4가 게이트 |
| M6 증식 (오디오 시스템 ✅ / 에셋·소품) | 🟡 |
| M7 폴리시 | 🔴 |

## 브랜치

| 브랜치 | 담당 | 상태 |
|---|---|---|
| `main` | 2층 CLI | M4 구현 예정 |
| `gemini/lane-character` | 3층 | 대기, 미착수 — 지시서 있음 |
| `gemini/lane-audio-content` | 3층 | 대기, 미착수 — 지시서 있음 |
| `gemini/lane-c-look`, `gemini/lane-e-assets-audio` | — | merge 완료, **삭제 대상** |

## 지금 열려 있는 작업 (우선순위)

1. **M4a `layout.js` + `house.js`** — 2층 CLI, `main`. 병렬 불가. 입력: `docs/spec/M4-layout.md`
2. **M4d 검증 스크립트** `tools/layout-check.mjs` + `render-check/m4-rooms.mjs` — 2층, M4a와 같은 세션
3. **M4c 방 3개 드레싱** — 3층 병렬 가능, **단 M4a merge 이후**
4. 걷는 캐릭터 / BGM·SFX 에셋 — 3층, **지금 바로 병렬 착수 가능** (M4와 파일 무충돌)
5. 벽 페이드 (`TAG.FADEABLE` 실제 투명도) — M4 이후
6. 룩 튜닝 (마젠타 치우침) — 아무 때나 5분

## 열린 결정 / 미확정

- 방 크기 6.0 m는 **계산값이지 체감값 아님** — 첫 주행 후 ±1 m 조정 가능
- 현관문(집 밖 출입) 만들지 여부 — M5 이후 판단
- 안개 far, 카메라 기본 거리 — M4 후 재확인 필요
