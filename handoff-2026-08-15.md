# 핸드오프 — 2026-08-15 (누적, 최신)

> **이 문서의 역할**: 지금 이 순간의 큐레이션 스냅샷. 새 세션(Claude 앱/CLI/Gemini 무엇이든)은 여기서부터 읽으면 된다.
> **가장 빨리 상태만 보고 싶으면 `docs/STATE.md`(60줄, 표만)를 먼저 봐라** — 이 문서는 그 상태의 배경/경위까지 담은 확장판이다.
> 문서 4종 역할 분담:
> - **`docs/STATE.md`** — 세션 시작 시 제일 먼저 읽는 60줄 요약. 서술 없이 상태 표만.
> - **`docs/roadmap.md`** — 설계 원본(마일스톤 정의·기술 결정·리스크표). 상태를 갱신하지 않는 불변 참조문서.
> - **`handoff-2026-08-15.md`**(이 문서) — "지금 뭐가 끝났고 뭐가 남았는지"의 최신 요약 + 경위. 매 세션 끝에 갱신.
> - **`dev-log.md`** — 원시 시간순 로그(append-only). "그때 무슨 일이 있었는지" 전체 경위가 필요할 때만 본다.
> - **`docs/handoff/gemini-lane-*.md`** — 특정 작업 레인 하나에 대한 자기완결적 지시서. 각각 git 브랜치 하나와 1:1 대응.
> - **`docs/spec/*.md`** — 좌표·수치의 단일 원본(예: `M4-layout.md`). Claude 앱(1층)이 작성, 숫자가 틀렸다고 판단되면 코드가 아니라 이 문서를 먼저 고친다.

---

## 0. 목표 (원문)

> 방 3개 + 거실을 포함한 집 · 사람 캐릭터 · 무료로 가져올 수 있는 asset 가져와서 수정/변경해서(색상, 단순화) 올리기 · 마우스나 키보드를 반영할 수 있는 인터랙션 · 기타 추가기능 · 배경음악 및 효과음 · hosting에 올리기

수익화 없음, 지인 피드백용. 상세 설계는 `docs/roadmap.md` 참고.

---

## 1. 저장소

- **GitHub**: https://github.com/huichul2-collab/260814_mini_game (public)
- **로컬 작업 경로**: `C:\Users\hcyang\claude\claude_project\260814_mini_games`
- **기본 브랜치**: `main`
- **게임 실행**: `game/index.html`을 정적 서버로 서빙(VS Code Live Server 등). ES 모듈이라 `file://` 직접 열기는 CORS로 안 됨.
- **개발 도구**: `tools/render-check/`(헤드리스 Chrome 렌더 검증 — `shot.mjs`, `move-check.mjs`, `audio-check.mjs`, `zoom-check.mjs`, `m4-rooms.mjs`, `collider-count.mjs`), `tools/check-imports.mjs`(import 경로·대소문자 검증), `tools/layout-check.mjs`(레이아웃 데이터 순수 node 정적 검증 7항목)

### 활성 브랜치

| 브랜치 | 상태 | 내용 |
|---|---|---|
| `main` | 진행 중 | M4 집 구조(거실+방3 뼈대) 완료 후 merge. 다음 작업 대기 |
| `gemini/lane-character` | **대기 중, 미착수** | 걷는 캐릭터(리깅+애니메이션) — 지시서: `docs/handoff/gemini-lane-character.md` |
| `gemini/lane-audio-content` | **대기 중, 미착수** | BGM/SFX 실제 에셋 — 지시서: `docs/handoff/gemini-lane-audio-content.md` |
| `gemini/lane-rooms` | **대기 중, 미착수** | 침실A·작업실·침실B 소품 드레싱 — 지시서: `docs/handoff/gemini-lane-rooms.md` (M4 완료 직후 신설) |
| `gemini/lane-c-look` | merge 완료(죽은 브랜치) | 후처리/안개/바깥지형. 삭제해도 무방 |
| `gemini/lane-e-assets-audio` | merge 완료(죽은 브랜치) | 에셋 파이프라인/오디오 시스템. 삭제해도 무방 |

---

## 2. 오케스트레이션 방식 (git 브랜치 릴레이)

여러 실행 표면(Claude Code CLI, Gemini/Antigravity, 그리고 앞으로 Claude 앱)이 **같은 GitHub 저장소를 공유하며 브랜치 단위로 작업을 분담**한다. 메커니즘:

1. 오케스트레이터(지금까지는 Claude Code CLI, 앞으로는 Claude 앱이 될 수 있음)가 `docs/handoff/gemini-lane-<이름>.md` 형식으로 **자기완결적 작업 지시서**를 작성한다. "자기완결적"이란: 이 대화 맥락을 전혀 모르는 상태에서 읽어도 실행 가능해야 한다는 뜻 — 프로젝트 배경, 건드려도 되는/안 되는 파일, 구체적 구현 방법, 검증 방법, 완료 기준을 전부 문서 안에 담는다.
2. 그 지시서를 `main`에 커밋하고, 같은 지점에서 `gemini/lane-<이름>` 브랜치를 만들어 push한다.
3. 실행 표면(Gemini 등)이 그 브랜치를 체크아웃해서 작업하고, **그 브랜치에만** 커밋+push한다. `main`으로 merge하지 않는다.
4. 오케스트레이터가 `git show <branch>:<path>`로 브랜치를 체크아웃하지 않고 파일 내용을 리뷰(같은 폴더에서 브랜치를 전환하면 §3의 사고가 재발하므로), 검증(`node --check`, `check-imports.mjs`, `render-check`)한 뒤 `main`에 merge한다.
5. 실제로 화면에 반영되게 **통합자(현재 Claude Code CLI)가 `main.js`에 연결**한다 — 레인 자체는 `main.js`/`index.html`을 건드리지 않는 게 규칙이라, 반드시 이 마지막 연결 단계가 필요하다.

**이게 왜 이렇게 생겼는지**: 처음엔 브랜치 없이 파일 범위만 나눠서 병렬 작업을 시켰는데, Gemini가 브랜치 대신 이 저장소 폴더에서 직접 `main`을 체크아웃해 작업하다가, 다른 레인으로 넘어가는 과정에서 커밋 안 된 변경사항이 사라지는 사고가 났다(자세한 경위: `dev-log.md` 2026-08-15 (7)(8)). 이후 모든 지시서에 **"-1. 작업 공간 분리" 섹션을 최상단에 강제**해서, 실행 표면이 원본 폴더가 아니라 **별도로 클론한 폴더**에서 브랜치를 체크아웃하도록 못박았다. `gemini/lane-character`, `gemini/lane-audio-content`부터 이 방식이 처음부터 적용됐다.

**Claude 앱으로 지휘를 옮기고 싶다면**: 위 1~5단계 중 "오케스트레이터" 역할만 CLI에서 Claude 앱으로 옮기면 된다. 앱 세션이 이 저장소를 읽고(`docs/roadmap.md`, 이 문서, `dev-log.md`), 새 지시서를 `docs/handoff/`에 작성해 커밋+브랜치 생성까지 하면, 그 뒤 실행(Gemini든 다른 CLI 세션이든)은 지시서만 보고 독립적으로 진행 가능하다. **단, 실행 표면이 push한 브랜치를 실제로 `main`에 merge하고 `main.js`에 연결하는 "통합" 단계는 누군가 반드시 사람 손(또는 에이전트)을 거쳐야 한다** — 지금까지 전부 그렇게 해왔고, 자동화된 적 없음.

---

## 3. 마일스톤 현황

| 마일스톤 | 상태 | 비고 |
|---|---|---|
| M0 — 기반 공사 | ✅ 완료 | git init, vendoring, 모듈 분리, 외곽선 제거+flat shading |
| M1 — 걸어다니는 방 1개 | ✅ 완료 | WASD 이동, 원-AABB 충돌(오차 0.000m 검증), 3인칭 추적 카메라, 휠 줌 |
| M2 — 룩 확정 | ✅ 완료 | 후처리(그레인+색보정+비네트)+안개+바깥지형. 색감은 튜닝 여지 있음(§5) |
| M3 — 캐릭터 & 텍스처 실습 | 🟡 절반 완료 | (a) 텍스처 실습: 에셋 파이프라인 완성+검증(`cubone.glb`를 KEEP 모드로 로드). (b) 걷는 캐릭터: **미착수**, `gemini/lane-character`에 지시서만 있음 |
| M4 — 집 구조 (방 3개+거실) | ✅ **완료** | `docs/spec/M4-layout.md` 스펙대로 `layout.js`+`house.js` 구현. `layout-check.mjs` 7/7, `m4-rooms.mjs`(실제 키입력 주행) 13/13 통과. 거실은 소품까지 완성, **침실A·작업실·침실B는 뼈대(바닥·벽·문)만** — 소품은 M4c |
| M4c — 방 3개 드레싱 | 🟡 대기 | `gemini/lane-rooms`에 지시서만 있음 |
| M5 — 배포 | 🔴 미착수 | M4c 완료가 게이트(사용자 지정 — "집 구조 완성 시 공개") |
| M6 — 증식(소품/오디오/상호작용) | 🟡 부분 진행 | BGM/SFX 재생 시스템 완성+검증. 실제 오디오 에셋은 `gemini/lane-audio-content`에 지시서만. 벽 페이드는 이제 착수 가능(문 있는 벽 구조 존재) |
| M7 — 폴리시 & 재배포 | 🔴 미착수 | |

### 지금 당장 남은 작업 (우선순위 순)

1. **M4c — 방 3개 드레싱** — `gemini/lane-rooms` 브랜치 대기 중, 지금 바로 착수 가능
2. **걷는 캐릭터** — `gemini/lane-character` 브랜치 대기 중, 지금 바로 착수 가능
3. **BGM/SFX 실제 에셋** — `gemini/lane-audio-content` 브랜치 대기 중, 지금 바로 착수 가능
4. **벽 페이드** — `TAG.FADEABLE`은 지금 카메라 거리 당김에만 쓰이고 실제 재질 투명도 페이드는 없음. 문 있는 벽 구조가 이제 있으니 Claude Code CLI가 `main`에서 착수 가능
5. **룩 튜닝** — 색보정이 sample1보다 마젠타 쪽으로 치우침, 바깥 지면 색 조정 여지. 버그 아님, 가벼운 작업이라 아무 때나 처리 가능
6. **M5 배포**(itch.io) — 1번 완료 후

### 병렬화 가능 여부 분석

M4 구조(임계경로)가 끝나서 **지금은 병렬 가능한 작업이 3개 동시에 열려 있다** — 이게 아래 분석의 핵심 변화.

| 항목 | 병렬 가능? | 이유 |
|---|---|---|
| **1. M4c 방 3개 드레싱** | ✅ 완전 독립 | 방마다 `world/rooms/<id>.js` 배타 소유, `layout.js`/`house.js`/`main.js` 안 건드림. **분기 완료** (`gemini/lane-rooms`) |
| **2. 걷는 캐릭터** | ✅ 완전 독립 | `player/character.js` 하나만 건드림, M4와 파일이 전혀 안 겹침. **분기 완료** (`gemini/lane-character`) |
| **3. BGM/SFX 에셋** | ✅ 완전 독립 | `audio/` + 소싱 작업, 다른 어떤 파일과도 안 겹침. **분기 완료** (`gemini/lane-audio-content`) |
| **4. 벽 페이드** | ✅ 이제 가능 | `camera/`만 건드림. 문 있는 벽 구조가 이제 존재해서 실제 테스트도 가능해짐 — Claude Code CLI가 `main`에서 담당 |
| **5. 룩 튜닝** | ✅ 독립적, 매우 가벼움 | `GradeGrainVignetteShader.js` 숫자 몇 개 + `exterior.js` 색 하나. 레인 만들 필요 없이 아무 때나 5분 작업 |
| **6. 배포** | ❌ 완전 순차 | 1번(방 드레싱) 끝나야 시작 가능 — 사용자가 "집 구조 완성 시 공개"로 게이트 |

**결론**: 1·2·3번 세 레인이 전부 동시에 열려 있고 서로도 안 겹친다(`world/rooms/*`, `player/`, `audio/`). 이미 브랜치까지 분기해뒀다(§1 참고). 4번(벽 페이드)은 Claude Code CLI가 `main`에서 이어갈 수 있는 다음 항목.

---

## 4. 이번 세션들에서 잡은 실제 버그 (재발 방지 레퍼런스)

빌드 단계가 없어서 이런 버그들이 런타임까지 조용히 통과한다 — 비슷한 클래스의 문제를 다시 만들 때 참고.

| 버그 | 증상 | 원인 | 커밋 |
|---|---|---|---|
| 비네트 화면 모서리 완전 검정 | 후처리 넣자마자 하늘/바깥지형까지 안 보임 | `smoothstep(0.8, 0.8-uVignetteSoft, ...)`의 edge0(0.8)>edge1(0.2) — GLSL 스펙 위반 | `c8170a6` |
| 바깥 지형이 방을 침범 | 방 오른쪽 하단(열린 모서리)이 깨져 보임 | `PlaneGeometry` 정점 간격(5)이 "반경 4 이내 평평" 하드 컷오프보다 커서 방 코앞 고리부터 접힘 | `4464d4e` |
| 오디오 전혀 재생 안 됨 + 로딩화면 안 사라짐 | "게임 시작" 눌러도 무반응 | `THREE.AudioLoader.load()`는 콜백 방식인데 `.then()` 체이닝 → `undefined.then()` 즉시 throw, 이게 `gate.js`의 페이드아웃 로직까지 막음 | `f4b1396` |
| Lane C 작업 "유실" (오판) | `main` 작업 디렉토리에서 파일이 안 보임 | 실제로는 정상 커밋됐는데 다른 브랜치에 있었을 뿐 — `main`만 보고 유실로 오판. 나중에 정정 | `0a06c61` |
| 공유 작업 디렉토리 충돌 | Claude 세션 편집 중이던 파일이 갑자기 이전 버전으로 보임 | Gemini가 같은 폴더에서 브랜치 전환 → HEAD가 같이 끌려감. 지시서에 작업공간 분리 강제한 뒤에도 M4 작업 중 `index.lock`/`HEAD.lock`으로 재발(스테일로 확인 후 안전 제거) | `dev-log.md` (16) |
| m4-rooms.mjs 테스트 8/13 연쇄 실패(게임 버그 아님) | "방 3→living 복귀" 구간부터 특정 좌표(-0.07,-3.05)에 멈추고 이후 전부 같은 자리에서 실패 | 테스트 스크립트가 거실 중앙(0,0)을 직행 목표로 삼았는데, D1 문의 반지름 감안 안전폭이 X[-0.93,-0.07]이라 0이 그 밖(0.07m 벗어남) — 그리디 워커가 문틀 오른쪽 끝에 접선으로 붙어버림. 문 통과 후 안전 경유지를 먼저 찍는 2단계 경로로 수정 | 13/13 통과 확인 |

---

## 5. 아직 안 건드린 것 / 알려진 튜닝 포인트

- **색보정 톤**: `game/src/render/post/GradeGrainVignetteShader.js`의 `uGain`(1.25,0.72,0.85)/`uLift`(0.06,0.02,0.08)가 `sample1.PNG`보다 마젠타 쪽으로 치우침. 숫자만 조정하면 됨
- **바깥 지면 색**: `game/src/world/exterior.js`의 `0x3d5240`(이미 한 번 밝힌 값). 조명 아래서 여전히 어둡게 느껴지면 더 밝힐 것
- **`game/assets/audio/test.mp3`(4.1MB)**: 테스트용, 커밋 안 함. 예산(BGM ≤1.5MB) 초과 사례이니 최종 BGM은 반드시 압축/트림
- **캐릭터**: 지금 `player/character.js`는 캡슐 + 방향표시 원뿔(노즈콘)뿐
- **방 크기 체감**: 6.0/5×4/4×5/4×4 m는 스펙의 계산값. `m4-rooms.mjs`로 "도달 가능·문 안 낌"은 확인됐지만 **미적으로 적당한지는 아직 사람이 안 봤다** — 드레싱(M4c) 후 재평가
- **안개 far·카메라 기본 거리**: 집이 14m 길이로 늘어난 뒤 재확인 안 함(`docs/spec/M4-layout.md` §5.4)
- **콜라이더 실측 24개**: 스펙 추정(~43)보다 적음 — 상인방 9개가 설계대로 non-solid라 그런 것뿐, 문제 아님

---

## 6. 다음 세션 시작 체크리스트

1. **`docs/STATE.md`부터 봐라** (60줄 요약, 이 문서보다 빠름)
2. `git status`, `git branch -a`로 대기 중인 레인 브랜치(`gemini/lane-rooms`, `-character`, `-audio-content`)에 진전이 있는지 확인
3. 있다면 `git show <branch>:<path>`로 리뷰(체크아웃 없이) → merge → `main.js` 연결 → `tools/render-check`로 검증
4. 없다면 벽 페이드(§3 "지금 당장 남은 작업" 4번) 또는 룩 튜닝 — 둘 다 `main`에서 바로 가능
5. 뭘 하든 **작업마다 바로 커밋+push**, `index.lock`이 보이면 지우기 전에 `git.exe`/Antigravity 프로세스부터 확인(`dev-log.md` (16))
6. 끝나면 `docs/STATE.md` + 이 문서 + `dev-log.md`를 갱신
