# 핸드오프 — 2026-08-15 세션 정리

> 이 문서는 `3d-game-handoff.md`(엔진/배포 결정 문서) 이후 진행된 실제 착수 세션의 결과와, Claude+Gemini 동시 운용 관련 논의를 정리한 것.

---

## 1. 게임 MVP — Three.js로 착수

**결정**: `3d-game-handoff.md`의 1순위(Unity)·2순위(Godot) 대신 **Three.js**로 시작.
**이유**: Cowork 세션은 파일 읽기/쓰기·셸만 가능하고 Unity/Godot 에디터 GUI를 조작할 수 없음. Three.js는 코드만으로 빌드·반복이 가능해 이 환경에 유일하게 실용적인 선택지였음. 장기 확장성(에셋스토어, 콘솔 이식)은 Unity/Godot이 우위이므로, 이 프로젝트가 커지면 재검토 여지 있음.

**구현물**: `game/` 폴더
- `index.html`, `main.js`, `vendor/three.module.js`, `vendor/three.core.min.js`, `vendor/OrbitControls.js`
- 문서 MVP 그대로 구성: 바닥1 + 벽3(정면 개방형, 위에서 비스듬히 내려다보는 구도) + 오브젝트(책상·의자·러그) + 상호작용 1종(스탠드 클릭 → 점등)
- 카툰 셰이딩: `MeshToonMaterial` + 4단계 그라디언트맵 + 인버티드 헐(inverted hull) 외곽선. `sample1.PNG` 톤을 참고해 노을빛 스카이돔 배경 추가.

**검증 한계 (약함, 확인 필요)**: 샌드박스 네트워크 allowlist 때문에 playwright/puppeteer/headless-gl 전부 설치 불가 → 실제 스크린샷으로 sample1과 톤 비교는 못 함. 문법 검사 + three.js 모듈 심볼 resolve만 정적으로 확인. **사용자가 직접 열어서 비주얼 확인 필요.**

**실행 방법**: ES 모듈 import라 `index.html` 더블클릭(file://)은 CORS로 안 열림. 로컬 서버 필요.
- Windows: `game` 폴더에서 PowerShell 열고 `python -m http.server 8000` 또는 `npx serve .` → `http://localhost:8000/`
- VS Code면 "Live Server" 확장으로 index.html 우클릭 → Open with Live Server
- itch.io 배포 시엔 이미 http로 서빙되므로 이 문제 없음

**Unity 에셋 재사용 가능 여부**: 원본 모델(.fbx/.obj/.glb)+텍스쳐를 따로 제공하는 팩은 Blender 경유 .glb 변환 후 재사용 가능(확신 상). Unity 전용 prefab/mat/셰이더그래프는 재사용 불가. Asset Store 라이선스에 "Unity 내 사용만" 조항이 있는 경우가 있어 구매 전 확인 필요(근거 약함). Kenney·Poly Pizza·Quaternius 같은 무료 gltf 소스가 지금 목적엔 더 마찰 적음.

---

## 2. 멀티 에이전트 워크플로우 — Claude + Gemini(Antigravity) 동시 운용

**배경**: 토큰/한도 부족 우려로 Claude(Opus 총감독 + Sonnet/Haiku)와 Gemini(Antigravity)를 동시에 쓰고 싶어함.

**Pro 요금제의 Opus**: 사용 가능하나 Sonnet/Haiku와 별도의(더 작은) 주간 한도로 관리됨. 정확한 수치는 Anthropic 비공개(확신 하) → `Settings > Usage`에서 실측 필요. Opus를 상시 대화용 "총감독"으로 쓰면 이 별도 한도가 빨리 소진될 가능성 높음(확신 중).

**권장 패턴**: Opus는 "계획"에만 짧게 쓰고 실행은 Sonnet — 공식 가이드가 권장하는 `/model opusplan`과 같은 철학. 단, `/model opusplan` 자체는 **Claude Code**(CLI / 데스크톱 앱의 Claude Code 탭 / 웹)에서만 동작하고, **Cowork에는 없음**(Cowork는 Claude Code 서페이스가 아님).

**Opus → Sonnet/Haiku 지시**: Claude Code 서브에이전트(`.claude/agents/*.md`, frontmatter의 `model` 필드)로 기본 지원. md 파일 본문 자체가 서브에이전트의 시스템 프롬프트가 됨.

**Opus → Gemini/Antigravity 지시**: 내장 기능 없음(별도 회사 제품). 2026-05-19 구글이 Gemini CLI를 은퇴시키고 **Antigravity CLI(`agy`)**로 통합 — 셸 명령 실행 + 서브에이전트 관리 지원. 로컬 환경에서 Claude Code가 Bash로 `agy`를 호출하는 것도 원리상 가능하나 **실제 검증한 조합 아님(확신 중하)**. 지금 쓰는 Cowork 세션(클라우드 샌드박스)에서는 Antigravity CLI 자체에 접근 불가.

**현실적 대안 — git 릴레이**: `docs/handoff/next-for-gemini.md` 같은 파일에 지시 작성 → 사용자가 Antigravity에서 그 파일 실행 → 결과가 레포에 반영되면 다음 Claude 세션에서 diff 읽고 이어감. 완전 자동은 아니지만(개입 1스텝 필요) 두 툴 표준 기능만으로 안정적.

**리스크**: 두 에이전트가 같은 코드베이스를 동시에 건드리면 머지 충돌·설계 불일치 가능성 실질적으로 큼. 작업 영역을 파일/모듈 단위로 명확히 나누거나, 한 번에 한쪽만 레포에 쓰게 하는 규칙을 처음부터 정해둘 것.

---

## 3. Open items

- [ ] 브라우저에서 `game/` 직접 열어 sample1 톤과 실제로 맞는지 확인
- [ ] Opus 주간 한도 실측 (`Settings > Usage`)
- [ ] Antigravity ↔ Claude Code 릴레이 방식 확정 (git 파일 릴레이 vs `agy` CLI 직접 호출)
- [x] `3d-game-handoff.md`의 "다음에 결정할 것" 항목과 연계해 아이템 증식 단계로 진행 (스코프 폭발 주의 — 문서 6번 리스크)

---

## 4. 아이템 증식 1차 — 소품 5종 추가 (2026-08-15 후속)

**구현물**: `game/main.js`에 상호작용 없는 순수 장식 오브젝트 5종 추가 (문서의 "고정 방 + 상호작용 1종" 제약 유지 — 새 상호작용은 추가하지 않음).

- **책장**(왼쪽 벽, `bookshelf` 그룹): 프레임 4판 + 선반 2단 + 컬러 박스 책 5권
- **화분**(`plant` 그룹): 테라코타 화분 + 이코사헤드론 잎 뭉치 2개, 방 앞쪽 왼쪽 모서리
- **벽 액자**(`frame` 그룹): 뒷벽에 테두리+캔버스 2겹 박스
- **머그컵**: 책상(`desk`) 자식으로 추가, 책상 위 좌측
- **방석**: 러그 옆 바닥에 실린더 1개

모두 기존 `makeMesh`/`toonMat`/`addOutline` 헬퍼 재사용 → 카툰 셰이딩·아웃라인 자동 적용됨. `node --check main.js`로 문법만 검증(이 세션도 headless 렌더 불가 — playwright 등 브라우저 자동화 도구 미설치, 이전 세션과 동일한 샌드박스 네트워크 제약으로 추정). **비주얼 확인은 여전히 사용자가 직접 해야 함.**

**배치 좌표 근거**: 방 크기 `ROOM_W=ROOM_D=3.6, ROOM_H=2.3`(벽 두께 0.12) 기준으로 벽 안쪽 면(x/z ±1.74)을 넘지 않도록 좌표 계산. 책상 상판 y=0.75, 바닥 y=0 기준으로 머그컵/방석 높이 맞춤.

**다음 후보** (미착수): 창문(벽에 구멍 뚫기는 현재 벽이 단일 Box라 별도 지오메트리 작업 필요), 커튼, 침대, 러그 패턴 텍스처, 고양이/인형 같은 캐릭터 소품.
